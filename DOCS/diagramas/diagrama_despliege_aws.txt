---
config:
  theme: mc
  layout: elk
---
flowchart TD
    classDef soft fill:#DBEAFE,stroke:#3B82F6,stroke-width:2px,color:#1E3A8A,font-weight:bold
    classDef db fill:#F3F4F6,stroke:#374151,stroke-width:2px,color:#111827,font-weight:bold
    classDef cloud fill:#FFEDD5,stroke:#EA580C,stroke-width:2px,color:#9A3412,font-weight:bold
    classDef aws fill:#FFF7ED,stroke:#EA580C,stroke-width:2px,color:#9A3412

    subgraph CLIENTES ["📱 / 💻 Dispositivos Cliente (Frontend)"]
        direction TB
        FLUTTER("📱 App Móvil (Flutter)<br>Clientes y Operarios"):::soft
        JFX("💻 App Escritorio (JavaFX)<br>Gerencia Empresa"):::soft
    end

    subgraph AWS ["☁️ Amazon Web Services (AWS) - Entorno Producción"]
        direction TB
        subgraph VPC ["🔒 Virtual Private Cloud (VPC)"]
            direction TB
            SERVER("🖥️ Instancia EC2<br>Servidor Central Java (Puerto 5000 TCP)"):::soft
            MYSQL[("🗄️ Amazon RDS<br>Base de Datos MySQL (Puerto 3306)")]:::db
            
            SERVER -- "JDBC SQL<br>(Tráfico por Red Interna AWS)" --> MYSQL
        end
    end

    subgraph FIREBASE ["🔥 Google Cloud (Firebase)"]
        direction TB
        FB_STORAGE("📁 Firebase Storage<br>(Hosting de Imágenes)"):::cloud
    end
    FLUTTER == "Sockets TCP / JSON<br>(A IP Pública de EC2)" ==> SERVER
    JFX == "Sockets TCP / JSON<br>(A IP Pública de EC2)" ==> SERVER

    FLUTTER -. "Upload Directo HTTPS<br>(Bypass del servidor)" .-> FB_STORAGE